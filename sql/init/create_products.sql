CREATE TABLE IF NOT EXISTS products(
    orderId         INTEGER,
    purchaseDate    TEXT,
    userId          INTEGER,
    product         TEXT,
    category        TEXT,
    price           REAL
);  